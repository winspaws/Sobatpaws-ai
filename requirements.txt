# Sobatpaws — Veterinary ML & AI Data Platform
# Python 3.10+

# Core data
pydantic==2.7.1
python-dotenv==1.0.1
# Hanya diperlukan bila menjalankan di Python 3.9 (sintaks union `str | None`).
# Untuk Python >= 3.10 paket ini tidak wajib.
eval_type_backport==0.2.0

# ML / data science
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.4.2

# API service
fastapi==0.111.0
uvicorn[standard]==0.30.1

# AI provider SDKs (opsional — aktifkan sesuai provider yang dipakai)
openai==1.30.1
anthropic==0.28.0

# DB (opsional, untuk memuat seed ke PostgreSQL)
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9

# Data generation & ML export (opsional)
# Generator inti (scripts/generate_*.py, validate_dataset.py) HANYA butuh stdlib.
# pyarrow dipakai build_ml_views.py untuk output Parquet; tanpa ini otomatis
# fallback ke gzip-CSV.
pyarrow==16.1.0
openpyxl==3.1.2
