# Ekosistem Satwa — Backend AI Services
# Docker Compose untuk development
# (Rebranded from Sobatpaws)
#
# Usage:
#   docker compose up -d              # Start services
#   docker compose logs -f            # Follow logs
#   docker compose down               # Stop services
#
# Environment variables:
#   Copy .env.example to .env and adjust

services:
  api:
    build: .
    container_name: ekosistemsatwa-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src
      - PYTHONUNBUFFERED=1
      # Defaults (overridden by .env if set)
      - EKOSISTEM_SATWA_AI_PROVIDER=${EKOSISTEM_SATWA_AI_PROVIDER:-local}
      - EKOSISTEM_SATWA_AI_AUGMENTATION_MODE=${EKOSISTEM_SATWA_AI_AUGMENTATION_MODE:-smart}
      - EKOSISTEM_SATWA_AI_TEMPERATURE=${EKOSISTEM_SATWA_AI_TEMPERATURE:-0.2}
      - EKOSISTEM_SATWA_AI_MAX_TOKENS=${EKOSISTEM_SATWA_AI_MAX_TOKENS:-800}
      - EKOSISTEM_SATWA_AI_DAILY_TOKEN_BUDGET=${EKOSISTEM_SATWA_AI_DAILY_TOKEN_BUDGET:-0}
      - EKOSISTEM_SATWA_AI_CACHE_TTL_SEC=${EKOSISTEM_SATWA_AI_CACHE_TTL_SEC:-3600}
      - EKOSISTEM_SATWA_AI_SKIP_LLM_CONFIDENCE=${EKOSISTEM_SATWA_AI_SKIP_LLM_CONFIDENCE:-0.82}
      - EKOSISTEM_SATWA_AI_FALLBACK_CHAIN=${EKOSISTEM_SATWA_AI_FALLBACK_CHAIN:-local}
      - EKOSISTEM_SATWA_LEARNING_BACKEND=${EKOSISTEM_SATWA_LEARNING_BACKEND:-jsonl}
      - EKOSISTEM_SATWA_AI_STORE_BACKEND=${EKOSISTEM_SATWA_AI_STORE_BACKEND:-jsonl}
      - EKOSISTEM_SATWA_SESSION_BACKEND=${EKOSISTEM_SATWA_SESSION_BACKEND:-jsonl}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-3-5-sonnet-latest}
      - LOCAL_LLM_MODEL=${LOCAL_LLM_MODEL:-llama3.2}
    volumes:
      # Persistent data: learning store, models, sessions
      - ekosistemsatwa_data:/app/artifacts
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

volumes:
  ekosistemsatwa_data:
    name: ekosistemsatwa_data
